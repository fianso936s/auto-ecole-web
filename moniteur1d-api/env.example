#
# Modèle de configuration (NE PAS mettre de vrais secrets ici)
# Créez un fichier `.env` à côté de ce fichier et remplissez les valeurs.
#

# --- App ---
NODE_ENV=production
PORT=3000
FRONTEND_URL="https://moniteur1d.com"

# --- Base de données (PostgreSQL Neon / Prisma) ---
DATABASE_URL="postgresql://USER:PASSWORD@host.neon.tech/dbname?sslmode=require&channel_binding=require"

# --- JWT (secrets longs et aléatoires) ---
JWT_SECRET="change_me_access_token_secret"
JWT_REFRESH_SECRET="change_me_refresh_token_secret"

# --- Admin (création auto au démarrage si ADMIN_PASSWORD est défini) ---
ADMIN_EMAIL="admin@moniteur1d.fr"
ADMIN_PASSWORD="change_me_strong_password"
ADMIN_FIRST_NAME="Admin"
ADMIN_LAST_NAME="System"

# --- Stripe (optionnel) ---
STRIPE_SECRET_KEY="sk_live_..."
STRIPE_WEBHOOK_SECRET="whsec_..."

# --- Email (optionnel: Resend) ---
RESEND_API_KEY="re_..."

# --- Cron / jobs (optionnel) ---
CRON_TOKEN="change_me_random_token"

# --- Stockage (optionnel: Cloudflare R2). Si vide => stockage local en dev ---
R2_ENDPOINT="https://<accountid>.r2.cloudflarestorage.com"
R2_ACCESS_KEY_ID="..."
R2_SECRET_ACCESS_KEY="..."
R2_BUCKET_NAME="mon-bucket"
R2_PUBLIC_URL="https://cdn.ton-domaine.com"

